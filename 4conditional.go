package main

import (
	"fmt"
	"bufio"
	"os"
)

var pl = fmt.Println

func main(){
	isAge := 18
	if (isAge >= 1) && (isAge<=18)
	{
		pl("")
	}else if (isAge == 48){
		pl("important ")

	}else {
		pl("not important")

	}
	
} 